/**
 * Main Entry
 * Layer: action
 *
 * GitHub Action entry point dispatching start/stop modes.
 *
 * Required ports:
 *   - poller.spawn
 *   - poller.kill
 *   - state.read
 *   - output.render
 */
export {};
